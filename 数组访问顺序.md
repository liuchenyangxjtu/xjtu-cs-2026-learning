# 数组访问顺序实验

## 起因（试着跑一下）

上一份笔记最后留了个问题：在 C 里删一个数组元素，到底要搬多少东西、有多贵？

我在课程大纲里翻到，《计算机体系结构》第 5、6 讲是 Memory and Cache。
就想自己写个小程序试试。

其实我本来想直接测"删元素"要多久。但试了一下发现不好测——
删元素的耗时太短，而且跟数据量、位置都有关系，单独拎出来量不准。

后来换了个更简单的办法：**不删，只改访问顺序**，看速度会不会变。
反正搬数据的本质就是"往内存里读读写写"，那访问顺序应该也有影响。

## 我做了什么（第一次做实验性认为）（还有点激动）

建了一个 8000 × 8000 的二维数组，差不多 244MB。
先往里填 i+j，免得全是 0。

然后把数组里所有元素加起来，做了两遍，加法的次数一模一样：

- **按行**：a[0][0] → a[0][1] → a[0][2] → ...
- **按列**：a[0][0] → a[1][0] → a[2][0] → ...

## 结果

程序在 8000 这一组跑出来的输出：

```text
数组大小：8000 x 8000，约 244 MB
正在填数据...
按行访问中...
按列访问中...

按行访问：0.116 秒
按列访问：0.317 秒
按列比按行慢 2.7 倍

两次的累加结果：511936000000 和 511936000000
结果一样，说明加法次数完全相同，只有访问顺序不同
```

两次的累加结果完全一样，说明这两遍真的只是顺序不同，干的事没有任何区别。
加法的次数也都是 6400 万次。

## 为什么会差这么多

我查了一下，原因是：**CPU 从内存读数据，不是一次读一个，而是一次读一整块。**

这一块叫 cache line，一般 64 字节，能装 16 个 int。
读进来之后会放在 CPU 的 cache 里，下次要用就在 cache 里拿，快很多。

- **按行访问**的时候，我读完 a[i][j]，下一个要读的 a[i][j+1]
  就在刚刚读进来的那一块里，直接从 cache 拿。
- **按列访问**的时候，下一个要读的是 a[i+1][j]。
  它在内存里隔了整整一行，8000 × 4 = 32000 字节，
  根本不在刚才那块里。只能重新去内存搬一大块。

所以慢的不是加法，是**反复去内存搬数据**。

## 我还试了别的数组大小

只改 `#define N` 这一个数字，重新编译再跑一遍：

| N        | 占用内存       | 按行（秒）     | 按列（秒）     | 倍率      |
| -------- | ---------- | --------- | --------- | ------- |
| 4000     | 34 MB      | 0.029     | 0.049     | 1.7     |
| 6000     | 137 MB     | 0.066     | 0.135     | 2.0     |
| **8000** | **244 MB** | **0.116** | **0.317** | **2.7** |
| 10000    | 381 MB     | 0.181     | 0.394     | 2.2     |

## 没想明白的

（这一节等上面那张表填完再写。如果 N 到 10000 的时候倍率反而降下来了，
 就写：我猜是内存带宽变成了瓶颈——两边都要等内存，差距就拉不开了。
 但我不确定，也没想好怎么验证。）

## 结论

程序快不快，不只取决于做了多少次运算，
还取决于**数据是怎么摆放的、按什么顺序去访问的**。

这两件事在代码上看起来一模一样，跑出来差了 **2.7** 倍。
