# 约瑟夫环

41 个人围成一圈，从第 1 个人开始报数，每数到第 3 个人就淘汰他，
然后从下一个人重新从 1 开始数。问最后剩下第几号。

答案：31 号。

---

## 第一版：真的把人删掉

思路:谁出列，就把他从数组里去掉，剩下的人接着数。

==**（写的时候有点诡异)**==：C 里面数组删一个元素，没有现成的函数，
==（**这一步其实挺好的）**==得自己写个循环，把后面的人一个一个往前挪一格：

```c
for(j=i;j<count-1;j++){
    people[j]=people[j+1];
}
```

删一个人就要挪一批人。41 个人，得删 40 次。

代码在 `约瑟夫环解法1.c`。

---

## 第二版：不删，只打标记

**==（再研究研究）（参考了网上一个老师的解法）==**人不动，谁出列就把他的编号改成 0。

数数的时候遇到 0 就跳过，用 `(i+1)%m` 一圈一圈地绕：

```c
if(f[i]!=0){
    k++;
}
```

这样确实不用挪数据了。代价是要一直空转，扫过那些已经变成 0 的位置。

代码在 `约瑟夫环解法2.c`。

---

## 两版比一下

|          | 第一版  | 第二版    |
| -------- | ---- | ------ |
| 出列的人怎么处理 | 真删掉  | 编号改成 0 |
| 要不要挪数据   | 要    | 不要     |
| 数组长度     | 越来越短 | 从头到尾不变 |

两个都是 O(n²)。n 个人要淘汰 n 次，每次都得在 n 个位置上花功夫，
乘起来就是 n²。

区别在于"贵"在哪：第一版贵在搬数据，第二版贵在空转。

---

## 我踩的两个坑

**一、变量声明写错位置**（**==？学校为啥不用c99）==**

C89 要求所有变量声明都放在代码块最前面。

我把 `int people[1000];`、`int count = n;` 写在了 `printf` 和 `scanf`
后面，用严格的 C89 编译直接报错，根本编译不过去。

**二、漏了一行 `i = 0;`（==真没看出来，让ai检查了一下）==**

初始化那个 for 循环跑完之后，`i` 已经等于 `n` 了。
我忘了重新把它置成 0，起点就错了。

有点意思的是，n=41、k=3 的时候答案居然还是 31。
因为 `(41+2)%41` 和 `(0+2)%41` 结果一样。纯属运气好，
中间要是多插一行代码就错了。

---

## 后来一直在想的一件事

写这两版的时候，我脑子里总是回到第一版那个"挪数据"。

代码里就一句 `people[j]=people[j+1]`，看起来特别轻巧。
但数组里删掉一个元素，底下居然要把后面所有元素全部搬一遍。

41 个人还不明显。要是 100 万个人呢？

一个在高级语言里看起来这么简单的操作，机器实际上干了多少活？

我想知道"搬数据"这件事到底有多贵。

---

## 接下来

**（有点意思）**

我在课程大纲里看到，《计算机体系结构》的第 5、6 讲是 Memory and Cache。

打算写个小程序测一下：数据怎么摆放、按什么顺序访问，
会不会影响程序跑的速度。
